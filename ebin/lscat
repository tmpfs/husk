#!/usr/bin/env node

var husk = require('../')
  .plugin([
    require('husk-stdin'),
    require('husk-exec')
  ]);

husk()
  .exec('ls')
  .exec(1, 'cat', console.log.bind(null, '[code: %s, signal: %s]'))
  //.pipe();
  .pipe(process.stdout)
