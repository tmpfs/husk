#!/usr/bin/env node

var husk = require('../')
  .plugin([
    require('husk-exec')
  ]);

husk()
  .exec('ls')
  // pipe `ls` stdout to `cat` stdin
  .fd(1)
  .exec('cat', console.log.bind(null, '[code: %s, signal: %s]'))
  .print()
  .run(true);
