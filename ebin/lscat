#!/usr/bin/env node

// Pipe stdout of a command to the stdin of the next command

var husk = require('../')
  .plugin([
    require('husk-exec')
  ]);

husk()
  .exec('ls')
  .exec('cat')
  .pipe(process.stdout)
